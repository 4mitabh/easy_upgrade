name: easy_upgrade_example
description: Minimal example for the easy_upgrade package.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.12.0
  flutter: ">=3.16.0"

dependencies:
  flutter:
    sdk: flutter
  easy_upgrade:
    path: ..

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
